name: Build Wheels

on: [push]

env:
  BUILD_TYPE: Release

jobs:
  linux_build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: true
    - name: Create Build Environment For JUCE
      run: cmake -E make_directory ${{runner.workspace}}/cxxbuild
    - name: Configure JUCE
      shell: bash
      working-directory: ${{runner.workspace}}/cxxbuild
      run: cmake $GITHUB_WORKSPACE -DCMAKE_BUILD_TYPE=$BUILD_TYPE
    - name: Build JUCE
      working-directory: ${{runner.workspace}}/cxxbuild
      shell: bash
      run: cmake --build . --config $BUILD_TYPE
    - name: Clean Python Wheel
      working-directory: ${{runner.workspace}}
      shell: bash
      run: python setup.py clean --all
    - name: Build Python Wheel
      working-directory: ${{runner.workspace}}
      shell: bash
      run: python setup.py sdist bdist_wheel

#  windows_build:
#    runs-on: windows-latest
#    steps:
#    - uses: actions/checkout@v2
#      with:
#        submodules: true
#    - name: Create Build Environment
#      run: cmake -E make_directory ${{runner.workspace}}/build
#    - name: Configure CMake
#      shell: bash
#      working-directory: ${{runner.workspace}}/build
#      run: cmake $GITHUB_WORKSPACE -DCMAKE_BUILD_TYPE=$BUILD_TYPE
#    - name: Build
#      working-directory: ${{runner.workspace}}/build
#      shell: bash
#      run: cmake --build . --config $BUILD_TYPE
#    - name: Test Lua 5.1
#      working-directory: ${{runner.workspace}}/build/Tests/Release
#      shell: bash
#      run: ./LuaBridgeTests51.exe

  macos_build:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: true
    - name: Create Build Environment For JUCE
      run: cmake -E make_directory ${{runner.workspace}}/cxxbuild
    - name: Configure JUCE
      shell: bash
      working-directory: ${{runner.workspace}}/cxxbuild
      run: cmake $GITHUB_WORKSPACE -DCMAKE_BUILD_TYPE=$BUILD_TYPE
    - name: Build JUCE
      working-directory: ${{runner.workspace}}/cxxbuild
      shell: bash
      run: cmake --build . --config $BUILD_TYPE
    - name: Clean Python Wheel
      working-directory: ${{runner.workspace}}
      shell: bash
      run: python setup.py clean --all
    - name: Build Python Wheel
      working-directory: ${{runner.workspace}}
      shell: bash
      run: python setup.py sdist bdist_wheel
  
